# 开发环境配置

# 服务器配置
server:
  port: 7090

# 开发环境特定配置
spring:
  # 数据库配置 - 开发环境（单个MySQL）
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/netdisk?serverTimezone=GMT%2B8&useUnicode=true&characterEncoding=utf8&autoReconnect=true&allowMultiQueries=true&useSSL=false&createDatabaseIfNotExist=true
    username: root
    password: hui200329
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      pool-name: HikariCPDatasource
      minimum-idle: 5
      idle-timeout: 180000
      maximum-pool-size: 10
      auto-commit: true
      max-lifetime: 1800000
      connection-timeout: 30000
      connection-test-query: SELECT 1
  
  # SQL 初始化配置 - 开发环境
  sql:
    init:
      # 开发环境可以设置为never或always
      mode: never
  
  # 邮件配置 - 开发环境
  mail:
    username: coder_zjh@qq.com
    password: yrfnoflesmsvjhjf
    properties:
      mail:
        debug: true
  
  # Redis 配置 - 开发环境（单个Redis）
  data:
    redis:
      database: 0
      host: 127.0.0.1
      port: 6379
      jedis:
        pool:
          max-active: 20
          max-wait: -1
          max-idle: 10
          min-idle: 0
      timeout: 2000

# 自定义项目配置 - 开发环境
project:
  folder: D:/ProjectCode/netdisk-all/netdisk-back/upload-file

# 日志配置 - 开发环境
log:
  root:
    level: debug

# 环境标识
dev: true

# QQ登录相关 - 开发环境
qq:
  app:
    id: 123456
    key: 123456
  url:
    redirect: http://localhost:7090/api/qqlogincalback

# 自定义项目配置 - 基础设置
project:
  ffmpeg:
    path: D:/ffmpeg/ffmpeg-2026-03-15-git-6ba0b59d8b-full_build/bin # FFmpeg bin目录路径,例如: C:/ffmpeg/bin (配置后无需依赖环境变量)
